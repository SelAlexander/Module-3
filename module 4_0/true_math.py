import math
from math import inf
def divide (first, second) :
    if second != 0:
        print(f"{first} : {second} = {first // second}")

    elif second == 0:
        print(math.inf)
        #print("∞")

